# <Title>

<!--
  Doctype: engineering — the rules the structure obeys. Copy to
  docs/<name>.md and fill in. Delete every comment, including this one;
  the document must read as if written directly.

  A spec is normative: when it and the artifact disagree, one of them is
  wrong — fix whichever lost, in the same change. Write each rule as a
  generating rule on structure ("a feature module depends on core
  interfaces only") or a predicate the artifact must keep true — never a
  mirror of what the artifact already says.

  required-fields: Doctype, Status, Scope, Last verified
  required-sections: Purpose

  rubric:
  - [ ] Purpose states the problem the structure solves before the
        structure.
  - [ ] Every section is a generating rule or a predicate, applicable to
        code the reader has never seen.
  - [ ] Values carry a reason and date, or name the constant that owns
        them.
-->

- **Doctype**: engineering
- **Status**: Active <!-- or: Superseded by <file> -->
- **Scope**: <!-- one sentence: what questions this document answers -->
- **Last verified**: <!-- YYYY-MM-DD — checked against the artifact -->

## Purpose

<!-- The problem this structure or mechanism solves, before the structure
     itself. -->

## <The structure, or the first rule>

<!-- One section per rule or structural part. -->

## Non-goals and deferred work

<!-- Optional: abstractions deliberately absent, with the reason. Delete
     when empty — an empty ledger is worse than none. -->
