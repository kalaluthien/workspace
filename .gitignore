# The workspace root tracks only workspace-level tooling.
# Project entries are independent git repositories and stay untracked here.
/*

# ...except these
!/.gitignore
!/.claude/
!/AGENTS.md
!/CLAUDE.md
!/docs/
!/hooks/
!/scripts/
!/spec/

# Machine-local Claude state never enters git.
.claude/settings.local.json
